Read the existing Electron + React project completely before making any changes.

Do NOT modify the UI.
Do NOT modify reminders.
Do NOT modify animations.
Do NOT modify Electron functionality.

I want to deploy only the React frontend to Vercel.

Please:

1. Separate the React renderer into a standalone Vite web application.
2. Keep the Electron desktop application unchanged.
3. Create a proper build script that outputs a standard "dist" folder.
4. Ensure npm run build generates the React production build for Vercel.
5. Preserve all pages (Landing, Login, Signup, Dashboard, Settings).
6. Remove only Electron-specific dependencies from the web build.
7. Do not change the UI or styling.
8. After finishing, tell me:
   - Build Command
   - Output Directory
   - Vercel settings