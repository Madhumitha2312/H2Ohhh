Read the entire project first before modifying anything.

The project is an Electron + React (electron-vite) application.

DO NOT modify:
- UI
- Dashboard
- Login
- Signup
- Reminder system
- Overlay
- Desktop notification
- Voice
- Animations
- Electron functionality

The only goal is to make the React frontend deployable on Vercel.

Tasks:

1. Keep the Electron desktop app completely unchanged.
2. Configure the React renderer as a standalone Vite web build.
3. Create a build script (example: build:web) that builds ONLY the React renderer.
4. Ensure the web build outputs a standard "dist" directory.
5. Update package.json scripts accordingly.
6. Do NOT remove Electron support.
7. Tell me exactly:
   - Build Command for Vercel
   - Output Directory
   - Any Vercel settings I must change.

Do not change the UI or any existing features.